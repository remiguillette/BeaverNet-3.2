version: 1

env:
  variables:
    VITE_CONTACT_API_URL: https://45toznwvvz6wjbsg72cvgx5gmq0nobqw.lambda-url.ca-central-1.on.aws/

frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
